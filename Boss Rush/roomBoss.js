class RoomBoss{    
    constructor(){
        //variables adjust the variables (same ones as roboBoss) to fit your character. Try to make it balenced


        //sprite // create the sprite for your boss. I would suggest to still make a boss as the player needs something to kill/defeat. Instead of it attacking, use the room as you planned, but make it teleport everytime it takes damage
        
    }

    display(){
        //for attacking, you can have blade/spike sprites flying back and forth across the screen that the player must dodge
            //you can create a cannon that shoots at the player

        //when it takes damage, it can teleport by setting the x and y poisitions to random(0, windowWidth) amd random(0, windowHeight)
    }
}